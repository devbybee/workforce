## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
# spring.datasource.url = jdbc:mysql://localhost:3306/note_app?useSSL=false&serverTimezone=EST5EDT
##
# development
#spring.datasource.url = jdbc:sqlserver://172.16.40.242;databaseName=db_MROSystem
#spring.datasource.username = sa
#spring.datasource.password = GMF2018#1

##
# production
spring.datasource.url = jdbc:sqlserver://172.16.40.100;databaseName=db_MROSystem
spring.datasource.username = usr-readonly
spring.datasource.password = p@ssw0rd

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.SQLServer2012Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

# Hibernate logging
logging.level.org.hibernate.SQL = DEBUG

# Initialize the datasource with available DDL and DML scripts
spring.datasource.initialization-mode=always



